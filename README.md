# OhRats UI

Shared visual language for OhRats Technologies. Core styles live in the fingerprinted `ohrats.css`; optional cross-product components such as the copy field are published as their own fingerprinted stylesheet.

Consumers use content-fingerprinted `/assets/*` URLs directly. Shared assets are immutable and do not have mutable aliases or redirecting compatibility URLs.
