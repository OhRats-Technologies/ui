# OhRats UI

Shared visual language for OhRats Technologies.

Consumers use stable `/current/*` URLs. Each deployment redirects those tiny uncached aliases to content-fingerprinted `/assets/*` files, so callers never manage cache versions manually. `/latest/*` remains as a compatibility alias.
