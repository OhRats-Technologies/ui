# OhRats UI

Shared visual language for OhRats Technologies.

Consumers use stable `/latest/*` URLs. Each deployment redirects those aliases to content-fingerprinted `/assets/*` files, so callers never manage cache versions manually.
